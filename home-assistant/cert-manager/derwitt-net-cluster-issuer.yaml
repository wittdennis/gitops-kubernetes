---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: derwitt-net-cluster-issuer
spec:
  acme:
    email: dennis@derwitt.de
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: derwitt-net-cluster-issuer-account-key
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef:
              name: derwitt-net-cloudflare-api-token
              key: api-token
