---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-blueprint-memos
spec:
  refreshInterval: "1h0m0s"
  secretStoreRef:
    name: vault-backend-platform
    kind: ClusterSecretStore
  target:
    name: authentik-blueprint-memos
    creationPolicy: Owner
    deletionPolicy: Delete
    template:
      engineVersion: v2
      mergePolicy: Replace
      data:
        memos.yaml: |
          version: 1
          metadata:
            name: derwitt-site-memos
          entries:
            - model: authentik_providers_oauth2.oauth2provider
              state: present
              id: memos_provider
              identifiers:
                client_id: memos
              attrs:
                name: Memos
                authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
                invalidation_flow: !Find [authentik_flows.flow, [slug, default-invalidation-flow]]
                redirect_uris:
                  - matching_mode: strict
                    url: https://notes.derwitt.site/auth/callback
                    redirect_uri_type: authorization
                include_claims_in_id_token: true
                sub_mode: hashed_user_id
                client_secret: "{{ .clientSecret }}"
                property_mappings:
                  - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-email"]]
                  - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-openid"]]
                  - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-profile"]]
                signing_key: !Find [authentik_crypto.certificatekeypair, [name, "Derwitt Signing Key (RSA)"]]
            - model: authentik_core.application
              state: present
              id: memos_application
              identifiers:
                slug: memos
              attrs:
                name: Memos
                protocol_provider: !KeyOf memos_provider
                meta_launch_url: https://notes.derwitt.site
                policy_engine_mode: all
            - model: authentik_policies.policybinding
              state: present
              identifiers:
                target: !KeyOf memos_application
                group: !Find [authentik_core.group, [name, "Memos Users"]]
              attrs:
                order: 0
  data:
    - remoteRef:
        conversionStrategy: Default
        key: authentik/oauth2-clients/memos
        property: client-secret
      secretKey: clientSecret
