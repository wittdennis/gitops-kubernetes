---
apiVersion: renovate-operator.mogenius.com/v1alpha1
kind: RenovateJob
metadata:
  name: github-all
spec:
  schedule: "15 6-19/2 * * 1-5"
  image: ghcr.io/renovatebot/renovate:44.56.4 # renovate
  parallelism: 5
  provider:
    name: github
  webhook:
    enabled: true
    baseUrl: https://api.renovate.derwitt.dev
    authentication:
      enabled: true
      secretRef:
        name: renovate-webhook-token
        key: token
    sync:
      enabled: false
  extraEnv:
    - name: LOG_LEVEL
      value: debug
    - name: RENOVATE_ONBOARDING_CONFIG
      value: |
        {
            "$schema": "https://docs.renovatebot.com/renovate-schema.json",
            "extends": ["local>wittdennis/renovate-config"]
        }
  githubAppReference:
    appIdSecretKey: client-id
    installationIdSecretKey: installation-id
    pemSecretKey: private-key.pem
    secretName: github-app-config
  secretRef: renovate-env
