---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: paperless-ngx-valkey-credentials
spec:
  refreshInterval: "1h0m0s"
  secretStoreRef:
    name: vault-backend-platform
    kind: ClusterSecretStore
  target:
    name: paperless-ngx-valkey-credentials
    creationPolicy: Owner
    deletionPolicy: Delete
  data:
    - remoteRef:
        conversionStrategy: Default
        key: /paperless-ngx/valkey-credentials
        property: default-password
      secretKey: default-password
    - remoteRef:
        conversionStrategy: Default
        key: /paperless-ngx/valkey-credentials
        property: replication-password
      secretKey: replication-password
    - remoteRef:
        conversionStrategy: Default
        key: /paperless-ngx/valkey-credentials
        property: paperless-password
      secretKey: paperless-password
