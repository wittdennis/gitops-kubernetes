---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mogenius-operator-dev-api-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend-platform
    kind: ClusterSecretStore
  target:
    name: mogenius-operator-dev-api-secret
    creationPolicy: Owner
    deletionPolicy: Delete
  data:
    - remoteRef:
        conversionStrategy: Default
        key: /mogenius-operator-dev/api-key
        property: api-key
      secretKey: api-key
