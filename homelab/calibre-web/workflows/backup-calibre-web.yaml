---
apiVersion: argoproj.io/v1alpha1
kind: CronWorkflow
metadata:
  name: calibre-web-backup
spec:
  schedules:
    - "26 2 * * *"
  concurrencyPolicy: "Replace"
  startingDeadlineSeconds: 0
  successfulJobsHistoryLimit: 1
  workflowSpec:
    entrypoint: main
    onExit: exit-handler
    podMetadata:
      labels:
        network-policy/allow-default-tenant-s3-traffic: apply
        network-policy/allow-kube-apiserver-traffic: apply
        network-policy/allow-backup-egress: apply
    securityContext:
      runAsNonRoot: true
      seccompProfile:
        type: RuntimeDefault
    serviceAccountName: calibre-web-workflows
    templates:
      - name: main
        dag:
          tasks:
            - name: scale-down
              arguments:
                parameters:
                  - name: name
                    value: homelab-calibre-web
                  - name: namespace
                    value: calibre-web
                  - name: replicaCount
                    value: "0"
                  - name: type
                    value: deployment
              templateRef:
                template: scale-workload
                name: scale-workload
                clusterScope: true

            - name: backup
              dependencies: [scale-down]
              templateRef:
                template: restic-backup
                name: restic-backup
                clusterScope: true
              arguments:
                parameters:
                  - name: host
                    value: calibre-web
                  - name: repository
                    value: s3:https://nbg1.your-objectstorage.com/pseud0random-backups/calibre-web
                  - name: secretName
                    value: calibre-web-backup-settings
                  - name: paths
                    value: /mnt/config /mnt/books
                  - name: retention
                    value: --keep-last 7
                  - name: podSpecPatch
                    value: |
                      volumes:
                        - name: config
                          persistentVolumeClaim:
                            claimName: homelab-calibre-web-config
                            readOnly: true
                        - name: books
                          persistentVolumeClaim:
                            claimName: homelab-calibre-web-books
                            readOnly: true
                      containers:
                        - name: main
                          volumeMounts:
                            - name: config
                              mountPath: /mnt/config
                              readOnly: true
                            - name: books
                              mountPath: /mnt/books
                              readOnly: true

      - name: exit-handler
        steps:
          - - name: scale-up
              arguments:
                parameters:
                  - name: name
                    value: homelab-calibre-web
                  - name: namespace
                    value: calibre-web
                  - name: replicaCount
                    value: "1"
                  - name: type
                    value: deployment
              templateRef:
                template: scale-workload
                name: scale-workload
                clusterScope: true
