---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: homelab-calibre-web
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: k8s-platform
  syncPolicy:
    automated:
      prune: true
  # the backup workflow scales this to 0 for the duration of the backup; without this, a
  # sync landing mid-backup reasserts replicas: 1 and races the backup pod's PVC attach
  ignoreDifferences:
    - group: apps
      kind: Deployment
      name: homelab-calibre-web
      namespace: calibre-web
      jsonPointers:
        - /spec/replicas
  destination:
    name: homelab
    namespace: calibre-web
  source:
    repoURL: oci://ghcr.io/wittdennis/helm-charts/calibre-web
    path: "."
    targetRevision: 1.1.2
    helm:
      valuesObject:
        env:
          - name: TZ
            value: Europe/Berlin

        persistence:
          config:
            enabled: true
            className: proxmox-data
            size: 1Gi
          data:
            enabled: true
            className: proxmox-data
            size: 50Gi

        route:
          enabled: true
          hostnames:
            - books.derwitt.site
          parentRefs:
            - group: gateway.networking.k8s.io
              kind: Gateway
              name: derwitt-site-gateway
              namespace: traefik
